from __future__ import annotations


def upload_placeholder_status() -> dict:
    return {"enabled": False, "reason": "upload endpoint intentionally deferred; chat accepts metadata placeholders only"}
